import classes from './Footer.module.css';

const Footer = () => {
  return (
    <footer className={classes.footer}>
      <small>Feito por Lucas Rafael Cipriani</small>
    </footer>
  );
};

export default Footer;
